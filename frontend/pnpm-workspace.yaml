allowBuilds:
  '@prisma/client': false # transitive dep of @better-auth/cli (schema-gen tooling only) — unused, this project is Postgres/Drizzle-only
  better-sqlite3: false # same — unused, no SQLite in this project
  esbuild: true
  sharp: false
  unrs-resolver: false
